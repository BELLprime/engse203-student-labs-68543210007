# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นายณัฏฐกิตติ์ รอดเรือน **รหัส:** 68543210007-9 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 12:31 | B2 2.2 | "กรองรายการ requesterName หรือ details มีข้อความที่พิมพ์ (ไม่สนพิมพ์เล็กใหญ่)" | อธิบายการใช้ `.toLowerCase()` และ `.includes()` ร่วมกันเพื่อค้นหาข้อความ และให้โค้ดตัวอย่างเงื่อนไขการกรอง | ตรวจสอบโค้ด ทำความเข้าใจการทำงาน และนำมาประยุกต์ใช้ในแอป ทดสอบแล้วหน้าเพจกรองคำได้ถูกต้อง |
| 13:54 | B3 3.2 | "จาก code ช่วยเพิ่ม style ของปุ่ม ทำเสร็จแล้ว หน่อยใหมัน ปุ่มdel" | แนะนำให้ใช้แท็ก `<div>` พร้อมกำหนด style CSS Flexbox (`display: 'flex'`, `gap: '8px'`) ครอบปุ่มทั้งสองเพื่อให้เรียงกันในแนวนอนอย่างเป็นระเบียบ | นำโค้ด <div> ไปครอบปุ่มในไฟล์ `RequestCard.jsx` และตรวจสอบบนหน้าเว็บพบว่าปุ่มจัดเรียงสวยงามตรงตามต้องการ |
| 14:00 | B3 3.3-3.4| "ช่วย guilde B3 3.3 -3.4 หน่อย" | ให้ขั้นตอนการเพิ่ม prop `onMarkDone` ส่งผ่าน `RequestList` ไปที่การ์ด และแนะนำวิธีเขียนฟังก์ชันเชื่อมกับ `updateRequestStatus` | นำหลักการไปทดสอบกด "ทำเสร็จ" แล้ว UI อัปเดตทันที พร้อมลองกด F5 รีเฟรชเพื่อยืนยันว่าข้อมูล persist ลง Local Storage จริง |
| 14:15 | B4 | "ช่วย guilde B4 ทีละ CP-B" | ให้ตัวอย่างการสร้าง Component `PriorityBadge` จากศูนย์ แนะนำการดักเคส "ไม่ระบุ" (Edge case) และวิธีเรียกใช้งาน | สร้างไฟล์ใหม่และนำโค้ดไปครอบแทนข้อความดิบในการ์ด พร้อมทำความเข้าใจ |

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ณัฏฐกิตติ์ รอดเรือน